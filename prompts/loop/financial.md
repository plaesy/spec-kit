---
description: "Cost structure, pricing, profitability, funding needs"
---

# `/loop:financial` command instructions

This is the **financial**-scoped entry point into `/loop`. Follow the full protocol
defined in `/loop`, with scope fixed to **financial**: Cost structure, pricing, profitability, funding needs.

Same 8-dimension scope set as `/assess:{scope}` (see `/assess`) — treat
`$ARGUMENTS` (e.g. `--focus <sub-area>`) as narrowing within this dimension, not
as a dimension selector; do not broaden scope to other dimensions unless the user
explicitly asks with a comma-separated scope on `/loop` itself.
