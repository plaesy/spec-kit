---
description: "Positioning, messaging, audience fit, competitive advantage, go-to-market"
---

# `/loop:marketing` command instructions

This is the **marketing**-scoped entry point into `/loop`. Follow the full protocol
defined in `/loop`, with scope fixed to **marketing**: Positioning, messaging, audience fit, competitive advantage, go-to-market.

Same 8-dimension scope set as `/assess:{scope}` (see `/assess`) — treat
`$ARGUMENTS` (e.g. `--focus <sub-area>`) as narrowing within this dimension, not
as a dimension selector; do not broaden scope to other dimensions unless the user
explicitly asks with a comma-separated scope on `/loop` itself.
