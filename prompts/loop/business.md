---
description: "Market fit, viability, business model, revenue potential, ROI"
---

# `/loop:business` command instructions

This is the **business**-scoped entry point into `/loop`. Follow the full protocol
defined in `/loop`, with scope fixed to **business**: Market fit, viability, business model, revenue potential, ROI.

Same 8-dimension scope set as `/assess:{scope}` (see `/assess`) — treat
`$ARGUMENTS` (e.g. `--focus <sub-area>`) as narrowing within this dimension, not
as a dimension selector; do not broaden scope to other dimensions unless the user
explicitly asks with a comma-separated scope on `/loop` itself.
