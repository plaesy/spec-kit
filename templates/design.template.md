---
# DESIGN.md — project design-system source of truth.
# Follows the Google Labs DESIGN.md spec (google-labs-code/design.md, version "alpha"):
# https://github.com/google-labs-code/design.md/blob/main/docs/spec.md
# See .plaesy/instructions/how-to-create-designmd.md for how to fill this in.
# Tokens here are normative; the markdown body below explains WHY, not what.
name: "{{PROJECT_NAME}}"
version: "alpha"
description: "{{ONE_LINE_DESIGN_SYSTEM_DESCRIPTION}}"
# omitted:
#   - section: "Elevation & Depth"
#     reason: "Flat design, no shadow system yet"
colors:
  light:
    primary: "#000000"
    secondary: "#000000"
    background: "#FFFFFF"
    surface: "#FFFFFF"
    text: "#000000"
    error: "#B3261E"
  dark:
    primary: "#000000"
    secondary: "#000000"
    background: "#000000"
    surface: "#000000"
    text: "#FFFFFF"
    error: "#F2B8B5"
typography:
  fontFamily: "{{FONT_FAMILY}}"
  h1: { fontSize: "2.5rem", fontWeight: 700, lineHeight: 1.2 }
  h2: { fontSize: "2rem", fontWeight: 700, lineHeight: 1.25 }
  body: { fontSize: "1rem", fontWeight: 400, lineHeight: 1.5 }
  caption: { fontSize: "0.75rem", fontWeight: 400, lineHeight: 1.4 }
spacing:
  unit: "4px"
  scale: [0, 4, 8, 12, 16, 24, 32, 48, 64]
rounded:
  sm: "4px"
  md: "8px"
  lg: "16px"
  full: "9999px"
components: {}
---

<!-- Body sections below follow the spec's fixed order (Overview, Colors,
     Typography, Layout, Elevation & Depth, Shapes, Components, Do's and Don'ts).
     Don't reorder or duplicate a heading. If a section genuinely doesn't apply,
     list it under `omitted` in the front matter above instead of leaving it
     empty here. -->

## Overview

<!-- One or two sentences: the design intent/personality of this product, not a
     feature list. E.g. "Architectural minimalism meets journalistic gravitas." -->

{{DESIGN_INTENT_SUMMARY}}

## Colors

<!-- Palette description and the semantic ROLE of each color token above — not a
     restatement of the hex values. What is `colors.light.primary` FOR? -->

## Typography

<!-- Font strategy: when to reach for h1 vs h2 vs body, why this typeface. -->

## Layout

<!-- Grid model and spacing rhythm — how `spacing.scale` maps to real layouts. -->

## Elevation & Depth

<!-- How visual hierarchy/z-order is communicated: shadows, borders, layering. -->

## Shapes

<!-- Corner-radius and general form language — how `rounded` tokens are applied. -->

## Components

<!-- One entry per reusable component FAMILY (Button, Card, Modal — not every
     variant), state conventions that apply system-wide. This is NOT the full
     component inventory — that's `docs/components.md` (generated by `/doc`,
     becomes an index + `docs/components/{name}.md` past 15 components). Link to
     it rather than duplicating it here. -->

| Component | States covered | Source |
| --- | --- | --- |
| Button | default, hover, focus, active, disabled, loading | `path/to/Button` |

## Do's and Don'ts

<!-- Concrete guardrails an agent can pattern-match against generated code. -->

- Do use `{spacing.md}` between form fields. Don't introduce a one-off pixel value.
- Do reuse an existing color token. Don't hand-pick a new hex value inline.

---

**Maintained by**: `/assess:design` (Design Spine, Mode 1) generates/updates this
file; `/assess:design` (Mode 2) audits code against it. Every UI-affecting
`/implement` run should read this file first so new components don't drift from
the tokens above. Treat a change here as a design decision, not a typo fix —
route non-trivial changes through `/optimize:design` so the audit re-runs.
